# Gemma GPU Service Dependencies
# PyTorch with CUDA support for GPU acceleration
# Note: These are heavy dependencies (~10GB+), only install for Gemma service

# Core ML Libraries
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.30.0

# Quantization support (optional, for memory efficiency)
bitsandbytes>=0.41.0

# FastAPI for serving
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0

# Hugging Face Hub (for model download)
huggingface-hub>=0.23.0

# Authentication (JWT verification for Workload Identity)
google-auth>=2.30.0

# Note: For Cloud Run deployment, ensure base image includes CUDA support
# Recommended base: nvidia/cuda:12.1.0-runtime-ubuntu22.04 or pytorch/pytorch:latest

