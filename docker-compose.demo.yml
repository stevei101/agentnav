version: '3.8'

services:
  agentnav-frontend:
    extends:
      file: docker-compose.yml
      service: agentnav-frontend
    environment:
      - ENVIRONMENT=demo

  agentnav-backend:
    extends:
      file: docker-compose.yml
      service: agentnav-backend
    environment:
      - ENVIRONMENT=demo
    # TODO: Add seed data script
    # command: ["sh", "-c", "python seed_demo_data.py && uvicorn main:app --host 0.0.0.0 --port 8080"]

  firestore-emulator:
    extends:
      file: docker-compose.yml
      service: firestore-emulator
